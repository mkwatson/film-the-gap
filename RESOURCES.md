# Official resource ledger and runtime gates

Last full pass: 2026-08-26 PT. The [official Devpost resources page](https://webmcp.devpost.com/resources) is the challenge-specific checklist; the [Official Rules](https://webmcp.devpost.com/rules) control when sources conflict. Recheck both pages because the draft standard, supporter material, FAQ, and browser implementations are moving during the challenge.

At the last same-day refresh, Devpost showed 1,717 participants and no published project gallery. The live count is context, not a selection metric; it reinforces the need for a structurally memorable interaction and a judge-legible deterministic path.

## Current status

| Requirement                                                            | Status                                       | Evidence or blocker                                                                                                                                                                                                                                                                                     |
| ---------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Read the official resources page                                       | Complete for the 2026-08-26 page             | Every substantive documentation and supporter link below was opened and assessed; optional credits, account creation, Discord, forms, and publishing actions were not performed.                                                                                                                        |
| Read the current WebMCP specification and first-party browser guidance | Complete on 2026-08-26                       | The current rendered draft is dated August 19, 2026; the surface is `document.modelContext`. See findings below.                                                                                                                                                                                        |
| Test in ChatGPT's in-app browser                                       | **Complete typed core flow passed natively** | ChatGPT desktop 26.820.60940 with GPT-5.6 Sol completed discovery, model-driven writes, dynamic registration/removal, evidence request, stale-quote refusal, exact-quote hold, and release against the visible page. Voice transport/delegation passes, but delegated Browser ownership remains a gate. |
| Test in Chrome with WebMCP enabled                                     | **Public native journey complete**           | Isolated Chrome 151 completed the compact dynamic tool lifecycle over public HTTPS, including a remote host, live Gateway proposal, truthful negative-evidence gate, 4/4 fixture fallback, stale refusal, exact hold, and release. A physical camera and Mark's normal Chrome profile remain.           |

Never collapse those last two rows into a generic "browser tested" claim. ChatGPT Site Tools add their own discovery, trust review, confirmations, tool lifetime, and model behavior on top of the browser implementation.

## Autonomous verification enablement

Read-only checks on 2026-08-26 found:

- Google Chrome 151.0.7922.174 is installed. At the latest check, the only running Chrome processes were isolated headless smoke-test profiles; the normal `Default` profile was not open.
- The one-off update completed: `/Applications/ChatGPT.app` is now the unified app (`com.openai.codex`, version 26.820.60940), is running, and `/Applications/Codex.app` is no longer installed. Pre-update threads can retain workers and browser trust metadata from 26.721.81911; start a fresh Codex chat after the update before treating browser-connectivity failures as system failures.
- macOS Screen Sharing is already reachable on the Mini's Tailscale address and the `openclaw` GUI session is logged in. The service is socket-activated, so an idle `screensharingd` process does not mean it is disabled. First try `vnc://opens-mac-mini.taild4d569.ts.net`; change Sharing settings only if authentication fails.
- Computer Use and its locked-use authorization plugin are installed and the helper service starts successfully. After the 2026-08-26 permission refresh, TCC reports Accessibility allowed for both `com.openai.codex` and `com.openai.sky.CUAService`, with Screen Capture allowed for the main app. A pre-update thread rejected browser control because its MCP/browser service was created before the app restart; a fresh post-update task connected successfully and completed the full native typed acceptance run.
- The ChatGPT Chrome Extension is installed and enabled in the `Default` Chrome profile, and its native messaging bridge manifest correctly authorizes it. This is useful for `@Chrome` control of an existing signed-in browser but is not documented as the exact Site Tools client.
- Live extension communication failed because no controllable normal-profile Chrome target was open. That no longer blocks the recommended path: Chrome DevTools MCP can launch a challenge-only persistent profile itself.
- The fresh unified-app task exposed a controllable ChatGPT in-app Browser target. Direct native Site Tools discovery, model-driven writes, dynamic tool changes, refusal, hold, and release all pass. A later Codex turn lacked the browser-control RPC despite the same plugin being installed; this is a control-plane regression, not evidence against the already-recorded native run.
- A signed-in Realtime WebRTC harness supplied real spoken audio, received an accurate transcript, and delegated to Codex. The delegated task did not inherit the UI-owned Browser binding, so it made no Site Tool calls and leaked no page/private state. A supported human handoff remains preferable to an ownership bypass.
- Camera automation can now run independently with `agent-browser` 0.29.0 and Chromium's synthetic media device. Physical microphone/camera permissions remain explicit clean-room gates.

The minimum enablement objective is therefore not broad machine access. It is two narrow, reviewable capabilities: the unified ChatGPT app's built-in Browser for exact judge-client acceptance and an autonomous challenge-only Chrome profile for continuous native-runtime verification. Keep microphone/camera actions explicit and scoped to the demo.

### Verification-route decision matrix

No single route proves browser correctness, model behavior, and the exact judged confirmation UX. Use the smallest layered stack that covers all three.

| Route                                                                                                | What it proves or enables                                                                                                                                        | Important tradeoff                                                                                                                                                              | Decision                                                                                                                                      |
| ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| Unified ChatGPT desktop built-in `@Browser` with GPT-5.6 Sol or Terra                                | Exact Site Tools discovery, trust review, page lifetime, model choice, and confirmations in the prescribed OpenAI client                                         | Rollout/account dependent; page-bound; consequential actions intentionally retain human confirmation; not exposed in Codex CLI/IDE                                              | **Mandatory final authority.** Run on the Mini daily once connected and repeat on the MBP as a clean-room check before submission.            |
| ChatGPT browser extension with `@Chrome`                                                             | Lets ChatGPT control the user's existing signed-in Chrome UI and reuse its sessions                                                                              | Broad access to that profile and history; easy to attach the wrong window; current docs describe Computer Use rather than promising native Site Tools consumption               | Optional only when an authenticated normal-browser state is genuinely needed. Do not count it as Site Tools proof.                            |
| ChatGPT Computer Use plus locked use                                                                 | Controls approved non-ChatGPT Mac apps and can continue an active trusted turn after macOS locks; useful for one-off GUI-only verification and remote continuity | Requires the plugin plus macOS Screen Recording and Accessibility grants; cannot automate ChatGPT itself, authenticate as an administrator, or approve privacy/security prompts | Enable once, grant only the challenge browser initially, and enable locked use. Keep structured browser/MCP tools primary.                    |
| ChatGPT Remote paired to a phone or another desktop                                                  | Start/continue Mini-hosted chats, approve actions, inspect diffs/screenshots, and steer from another device while retaining the Mini's tools and credentials     | Host must remain awake, online, signed in, and running ChatGPT; same account/workspace and one-time QR pairing are required; it adds control-plane reach, not browser evidence  | Pair during the same one-off GUI session and enable `Keep this Mac awake`; useful convenience, not a testing authority.                       |
| ChatGPT Work cloud browser                                                                           | A remotely hosted computer that can read, click, and type on public sites without relying on the Mini's GUI                                                      | Separate machine/session; cannot reach Mini-only origins; current Site Tools docs do not establish it as the exact WebMCP client                                                | Useful public-deployment smoke test, not an acceptance gate.                                                                                  |
| Chrome DevTools MCP 1.8.0 launching a persistent challenge-only Chrome profile with `WebMCP` enabled | Autonomous headed navigation, screenshots, DOM/accessibility, console/network/performance inspection, plus native `list_webmcp_tools` and `execute_webmcp_tool`  | Developer automation, not ChatGPT model behavior; profile must remain isolated because the MCP client can read and operate it                                                   | **Primary SSH-first development loop.** Pin the current release, disable usage/CrUX telemetry, and recheck the release daily before updating. |
| Chrome DevTools MCP `--autoConnect` to the normal Chrome profile                                     | Reuses live cookies and all open windows after one-time remote-debugging approval                                                                                | Highest privacy/cross-window risk and can select the wrong profile                                                                                                              | Avoid unless a signed-in third-party flow cannot be reproduced in the isolated profile.                                                       |
| Standalone persistent Chrome with a remote-debugging port or LaunchAgent                             | Decouples browser lifetime from a Codex/MCP client and makes manual attachment easy                                                                              | Extra lifecycle machinery; any permitted local process may control the exposed browser; remote debugging requires a non-default profile                                         | Reserve fallback if MCP-managed browser lifecycle proves unreliable.                                                                          |
| Chrome DevTools WebMCP panel                                                                         | Native manual schema, invocation, status, input/output, and dynamic-tool diagnostics                                                                             | No agent or natural-language selection                                                                                                                                          | Mandatory debugging and useful demo/README evidence, but not the hero experience.                                                             |
| Google's Model Context Tool Inspector 1.9.13                                                         | Manual tool calls, monitoring, and current Gemini-driven natural-language tool selection in Chrome                                                               | Developer-only security boundary; Gemini behavior is not ChatGPT behavior                                                                                                       | Install in the isolated profile; use for ambiguous-prompt and recovery evals and visible Google-product leverage.                             |
| Devpost-linked independent WebMCP Extension                                                          | Satisfies the challenge resource checklist and offers a second manual inspection surface                                                                         | Independent implementation, less authoritative than native Chrome DevTools, and not a model by default                                                                          | Secondary compatibility check only.                                                                                                           |
| Existing isolated headless Chrome/agent-browser tests                                                | Fast deterministic positive/negative feature gates, rendering checks, and CI-friendly regressions                                                                | Headless execution cannot prove human-in-the-loop UX or exact client behavior                                                                                                   | Keep on every meaningful change.                                                                                                              |
| Cloudflare Browser Run WebMCP lab over CDP                                                           | Independent hosted Chrome session, remote live view, scale, and a meaningful Cloudflare sponsor integration                                                      | Experimental lab, currently documented with Chrome 146, session/meter limits, and not the exact judge client                                                                    | Add after public deployment as an independent regression lane, not the local source of truth.                                                 |
| Chrome WebMCP origin-trial token                                                                     | Lets ordinary visitors use the live deployed origin without enabling a local flag                                                                                | Time-limited enrollment/configuration; does not affect ChatGPT support                                                                                                          | Add when the public production origin is stable.                                                                                              |
| ChatGPT Record & Replay skill                                                                        | Replays a stable browser/UI acceptance workflow after recording it once                                                                                          | Brittle when UI changes; confirmations remain; replay is not native WebMCP evidence by itself                                                                                   | Record only after the exact Site Tools path is stable.                                                                                        |
| API function-tool/eval mirror generated from canonical page schemas                                  | Cheap model-selection, argument, order, refusal, and recovery testing at scale                                                                                   | Different tool transport and authority model; hand-maintained schemas can drift                                                                                                 | Generate from one canonical contract and label results supplementary.                                                                         |
| Remote MCP/MCP-B mirror                                                                              | Tests continuation beyond the page and cross-client interoperability                                                                                             | Not page-bound WebMCP and can blur the submission's core claim                                                                                                                  | Optional ambitious rung only when it creates a coherent user journey.                                                                         |
| Generic cloud browsers, BrowserStack/Sauce, AppleScript, or exposed VNC/noVNC automation             | Additional browser/GUI reach                                                                                                                                     | Flags/extensions and Site Tools are awkward; GUI automation is brittle; exposed remote desktops add security risk without new proof                                             | Reject for the primary loop. Use Apple's Screen Sharing only for narrow setup and human approvals.                                            |

### Recommended operating stack

1. Preserve the current deterministic Vitest, strict TypeScript, production-build, ordinary-browser, and native headless Chrome gates.
2. Add Chrome DevTools MCP 1.8.0 globally with a dedicated persistent profile outside the repository, `--categoryExperimentalWebmcp=true`, and `--enable-features=WebMCP`. This is the autonomous SSH-first loop.
3. Install Google's current Model Context Tool Inspector and the challenge-linked extension only in that isolated profile.
4. Keep using the current unified ChatGPT desktop app with Sol or Terra, require `Site Tools` in the address bar before accepting a run as exact evidence, and repeat the typed core flow after material tool-contract or client-version changes.
5. Install/enable Computer Use, grant its helper Screen Recording and Accessibility in macOS, allow only the challenge browser initially, and enable locked use. This extends remote GUI reach but does not and must not automate ChatGPT's own security UI.
6. Pair ChatGPT Remote to Mark's phone or MBP under Settings > Connections > Control this Mac, and enable `Keep this Mac awake` while plugged in.
7. Keep human confirmation for communications, holds, purchases, permissions, and any other consequential boundary. Autonomy should reach the confirmation gate reliably, not bypass it.
8. After public deployment, enroll the stable origin trial and add Cloudflare Browser Run as an independent hosted regression lane. Record & Replay and scalable API evals come only after the primary contract stabilizes.

### Current capability inventory

| Capability                         | Ready now                                                                                                                                                                                                                                           | Gap or next action                                                                                                                                                 |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Local implementation and tests     | Node 26.3.1, pnpm 11.24.0, strict TypeScript, 70 app behaviors plus 7 room-Worker and 12 merchant-Worker behaviors, Next.js production builds, two Wrangler dry-run bundles, and isolated native-browser automation.                                | Physical-device, permanent-origin, and exact-ChatGPT clean-room checks remain.                                                                                     |
| Native WebMCP lab                  | Chrome 151 completes the compact public HTTPS loop plus synthetic-camera capture, live structured vision, truthful negative gate, native inspect, stale refusal, exact hold, and release.                                                           | Connect Mark's normal Chrome 151 profile for interactive DevTools/extension evidence and repeat with a physical camera.                                            |
| ChatGPT Site Tools                 | Unified app 26.820.60940 with GPT-5.6 Sol passed native discovery, model-driven writes, dynamic tool changes, stale refusal, exact-quote hold, and release on the prior three-tool read contract.                                                   | Re-run the current reduced two-tool initial contract, then test navigation/reconnect, a supported Voice-to-UI-owned-Browser handoff when exposed, and Mark's MBP.  |
| Voice, audio, and media processing | FFmpeg/ffprobe 9.0.1, real Realtime WebRTC/Opus transcription/delegation, opt-in browser camera capture, JPEG/SHA-256 provenance, server digest verification, live AI Gateway structured vision, human review, and selected-frame publication pass. | Delegated Voice lacks the UI-owned Browser; a rights-cleared physical-device acceptance remains.                                                                   |
| Git and GitHub                     | GitHub CLI and the GitHub connector are authenticated; local challenge-period history is intact.                                                                                                                                                    | This repository has no remote. Creating a public repository or pushing remains an explicit external action for Mark to approve.                                    |
| Vercel                             | CLI auth, an isolated project, automatic OIDC, AI Gateway, a protected preview, stable preview alias, Functions, logs, and deployment APIs all pass.                                                                                                | The final no-login origin, automation-bypass cleanup, exact ChatGPT run, and production promotion remain; do not top up Gateway credits without approval.          |
| Cloudflare                         | Wrangler 4.126.0, Workers types 5.20260827.1, and `@cloudflare/vitest-plugin` 1.1.0 back a tested SQLite Durable Object. A temporary Worker passed public HTTPS/WSS, CORS, state recovery, and the complete two-client flow.                        | The temporary account is disposable. A permanent authenticated deploy and physical phone/network acceptance remain.                                                |
| Formal verification                | Elan is installed; the local `math/deliverable` sibling project pins and successfully runs Lean 4.27.0, Lake 5.0.0, and a specific mathlib revision.                                                                                                | There is intentionally no global default toolchain. Reuse an isolated challenge proof project when a visible invariant earns this rung.                            |
| Cloud and data tooling             | Google Cloud SDK, AWS CLI, PostgreSQL 18.4, SQLite 3.51, Python 3.14, uv 0.11.21, Deno 2.9.3, ImageMagick 7.1.2, and current web research are locally available.                                                                                    | Authenticate or add services only for a selected load-bearing capability; broad credential installation now would increase risk without improving the golden path. |

The operating rule is capability-on-demand: keep current docs and deterministic local verification universal, then connect the narrow deployment, media, commerce, or proof capability required by the next experiment. Installing every sponsor CLI up front would not make the agent meaningfully more autonomous; connected judged-client control does.

### E7 realtime platform decision — 2026-08-26 PT

Current first-party documentation was re-read before implementation:

- [Vercel's June 22 WebSocket guidance](https://vercel.com/kb/guide/do-vercel-serverless-functions-support-websocket-connections) says Functions now support WebSockets, but connections are pinned to one Function only until its maximum duration and durable cross-instance state needs Redis. The current [Next.js WebSocket guide](https://vercel.com/kb/guide/real-time-chat-websockets) additionally uses experimental `experimental_upgradeWebSocket()`, `vercel dev`, Redis pub/sub, and mandatory reconnect. The older [Vercel limits page](https://vercel.com/docs/limits) still says Functions cannot act as WebSocket servers; this documentation conflict is recorded rather than silently resolved in Vercel's favor.
- [Cloudflare's Hibernation WebSocket guidance](https://developers.cloudflare.com/durable-objects/best-practices/websockets/) gives one room a single ordered coordinator while connections survive object eviction. [Declarative Durable Object exports](https://developers.cloudflare.com/durable-objects/reference/durable-objects-migrations/) now make SQLite the required/recommended backend for a new namespace. [The current testing recipe](https://developers.cloudflare.com/durable-objects/examples/testing-with-durable-objects/) runs the same Durable Object and hibernatable sockets inside workerd through `@cloudflare/vitest-plugin`.
- [Workers Vitest integration](https://developers.cloudflare.com/workers/testing/vitest-integration/) and [configuration](https://developers.cloudflare.com/workers/testing/vitest-integration/configuration/) are current as of 2026-08-27 PT: `@cloudflare/vitest-plugin` 1.1 runs tests in workerd and explicitly supports test-only service bindings. The new `@msw/cloudflare` recipe did not intercept a request initiated inside this Durable Object in our probe, so the authoritative UCP test uses a strict in-process merchant service binding and leaves production bindings untouched.
- The [current Durable Object limits](https://developers.cloudflare.com/durable-objects/platform/limits/) cap a stored key/value at 2 MB and an inbound WebSocket message at 32 MiB. The room therefore keeps a stricter 1.2-million-character protocol ceiling; the camera rung must reduce its public JPEG below the stored-state budget before remote publication.

Decision: **Cloudflare Durable Objects own E7 room authority; Vercel remains the judge-facing Next.js/AI surface.** This is one coherent cross-sponsor architecture, not dual hosting. A Vercel room would need an experimental Next upgrade API plus Redis/pub-sub and periodic reconnect to reproduce what one Durable Object owns directly, adding more moving parts to the three-minute hero. The choice can still be revisited if public preview behavior contradicts the local runtime evidence.

## Historical Rung 1 native run

On 2026-08-26 PT, revision worktree before the first feature commit passed a complete native WebMCP loop at `http://localhost:3000` in agent-browser's isolated Headless Chrome 150.0.0.0 runtime:

1. The page registered and the browser discovered only `inspect_live_show`, `set_buying_mandate`, and `inspect_current_lot` initially.
2. Native `executeTool` invoked `set_buying_mandate`; the visible page became unresolved and `request_host_evidence` appeared.
3. Native `executeTool` invoked the evidence request; the request appeared in the visible host queue and that now-redundant tool disappeared.
4. The visible host control disclosed no repair; the final condition became supported and `reserve_current_lot` appeared.
5. Native `executeTool` created an agent-attributed reversible hold; `reserve_current_lot` disappeared and `release_current_lot` replaced it.

The same run verified desktop and 390 px layouts, meaningful accessibility snapshots, no framework error overlay, no recorded page errors, no horizontal mobile overflow, and no console errors beyond development notices. Formatting, ESLint, strict TypeScript, 12 Vitest behaviors, and the Next.js 16.3.3 production build also passed.

This is stronger than a polyfill or handler-only test, but it is still supplementary: it does not prove Mark's current Chrome 151 profile, model-driven tool selection, ChatGPT's trust/confirmation layer, or Voice-to-Site-Tools composition. Those remain explicit gates below.

## Rung 2 native product run

On 2026-08-26 PT, the privacy-membrane worktree passed the full product flow in agent-browser's native isolated Headless Chrome 150.0.0.0 runtime:

1. The browser discovered exactly `inspect_live_show`, `set_evidence_requirements`, and `inspect_current_lot` initially.
2. Native `executeTool` set four product-evidence requirements. The page showed a privacy receipt, no buyer ceiling appeared in the page or result, and `request_host_evidence` registered.
3. Native execution queued repair-history evidence. Seven anonymous demo signals became eight queued requests and the redundant request tool disappeared.
4. One visible host answer changed the aggregate to eight resolved requests and registered `reserve_current_lot`.
5. A stale `$400` call against the public `$423` all-in quote was refused without a hold and returned the exact recovery action.
6. An exact `$423` call created the attributed hold; reserve disappeared and `release_current_lot` replaced it.
7. Native release restored the valid pre-hold tool surface.

The ordinary-browser UI completed the corresponding share → request → answer → hold → release flow. Desktop and 390 px checks showed no framework error overlay or horizontal overflow. Fresh profiles in the installed Chrome 151.0.7922.174 rendered `Site Tools live` with `--enable-features=WebMCP` and `Browser fallback` with the feature disabled.

Strict schema tests reject a supplied `maxAllInPrice`; recursive contract tests reject forbidden private-value fields in registered definitions and snapshots. Data minimization does not prove zero statistical inference. The seven other agents are a transparent deterministic fixture, not a live room. ChatGPT's complete typed core path now passes; autonomous Voice-to-Site-Tools, repeated navigation/reconnect, normal-profile Chrome, and a clean-room MBP run remain pending.

## Camera provenance native acceptance

On 2026-08-26 PT, `agent-browser` 0.29.0 drove the production build in Headless Chrome 150 with Chromium's synthetic video source:

1. Buyer and host tabs synchronized four public requirements and one normalized request for eight private decisions.
2. The host camera remained off until `Start camera`; `getUserMedia` requested `audio: false`, then exposed the synthetic feed.
3. Capture produced one 960×540 JPEG, computed SHA-256 locally, stopped the stream, and required a separate host `none`/`repaired` attestation.
4. Publishing `none` moved the buyer to 3/3 evidence supported. Shared state and native `inspect_current_lot` returned only the bounded descriptor—frame ID, UTC time, dimensions, digest, kind, and label—not raw bytes or a blob URL.
5. Native `document.modelContext.getTools()` showed `reserve_current_lot`; native `executeTool` held at exact `$423`, replaced reserve with `release_current_lot`, and released successfully.
6. Desktop host-camera/captured and buyer-ready screenshots plus a 390×844 host screenshot showed no framework overlay or horizontal overflow. Browser console and page-error logs were empty.
7. Component tests independently cover explicit activation, video-only constraints, permission denial/retry, stream cleanup, deterministic hashing, strict room rejection of raw media/malformed digests, and WebMCP output privacy.

Five machine-local acceptance screenshots are retained outside the repository, from host request through mobile resolved state.

This proves browser media lifecycle, bounded provenance, cross-tab propagation, and the native tool transition. It does not prove that a physical scene is authentic, that a model can determine repair history, or that raw/continuous video is understood. The digest is only a byte fingerprint.

## Reviewed visual evidence native acceptance

On 2026-08-26 PT, the next production build added AI SDK 7.0.79 and a dynamic `POST /api/evidence/propose` route. The implementation was checked against the installed AI SDK 7 package docs/source plus Vercel's current [AI Gateway model catalog](https://ai-gateway.vercel.sh/v1/models), [model fallback](https://vercel.com/docs/ai-gateway/models-and-providers/model-fallbacks), [authentication](https://vercel.com/docs/ai-gateway/authentication-and-byok), and [provider option](https://vercel.com/docs/ai-gateway/models-and-providers/provider-options) docs.

On 2026-08-27 PT, the installed package was refreshed to AI SDK 7.0.83 after re-reading its bundled docs/source. The strict structured-output route tests, TypeScript, production build, and full repository gate passed under pnpm 11.24.0; this updates the implementation baseline without changing the host-review authority boundary or the measured model-routing claims below.

1. The route accepts one JPEG up to 650 KB, validates a derived camera frame ID and bounded dimensions, recomputes SHA-256 from received bytes, and rejects mismatches before any model call.
2. With Gateway auth, AI SDK uses the Gateway-default plain model ID `openai/gpt-5.6-sol`, model fallbacks `openai/gpt-5.6-terra` and `openai/gpt-5.6-luna`, and `zeroDataRetention: true`. The live catalog reports image input for all three. Image-bearing request messages and bodies are excluded from retained AI SDK step results.
3. The structured output can describe only base visibility, a visible surface signal, confidence, visible details, a concise summary, and a suggested next view. The prompt forbids historical repair, authenticity, identity, intent, price, ownership, and safety claims.
4. A proposal is never authoritative. The host must accept or correct it; a manual path records `modelId: null` and a distinct `manual` decision. The state machine rejects cross-frame replay, unclear views, missing selected images, and conflicts between a visible possible-repair signal and a no-repair history attestation.
5. The current local environment has no `AI_GATEWAY_API_KEY` or `VERCEL_OIDC_TOKEN`, so the browser correctly received `gateway-unconfigured` and completed manual review. API tests inject a valid model result to verify the exact provider configuration, structured contract, and provider-error fallback without claiming a live inference.
6. The host explicitly published the one selected JPEG after review. The buyer rendered that same frame and changed to 4/4 supported. The continuous camera feed stayed local.
7. Native `document.modelContext.executeTool` returned the frame provenance, host-reviewed finding, historical limitation, and `selectedFramePubliclyVisible: true`, while the JSON contained no image data. Exact `$423` reserve, dynamic tool replacement, and release passed again.
8. Formatting, zero-warning lint, strict typecheck, 41 tests across 9 files, and the Next.js 16.3.3 production build pass. Browser console and page-error logs were empty.

The new machine-local screenshots are retained outside the repository. The automated camera supplied a Chromium synthetic test image, so those screenshots prove plumbing and review boundaries—not that the visual assertion matches a physical snowboard. A live authenticated model call against a staged, rights-clear physical product frame remains mandatory before any submission claim about GPT-5.6 visual interpretation.

## Current compact Site Tools acceptance

On 2026-08-26 PT, E9.1 rechecked the live OpenAI Site Tools documentation, Chrome WebMCP best practices/eval guidance, and the current rendered draft before changing the contract. Chrome explicitly warns that overlapping tools increase selection difficulty and recommends returning only the minimum information needed for the next action.

1. The duplicate `inspect_current_lot` tool was removed. `inspect_live_show` is now the single read contract; the initial page exposes exactly it plus `set_evidence_requirements`.
2. Descriptions now state the positive operation and usage point. Dynamic registration continues to expose only state-valid mutations.
3. Common results are capped by a deterministic test at 3,500 serialized characters. They retain the exact quote, evaluated evidence, compact frame/review provenance, aggregate request, hold, next action, available tools, and privacy receipt while removing activity history, duplicate state, raw JPEG data, and the model's superseded proposal.
4. Native isolated Chrome discovered `2 → 3 → 2 → 3 → 2 → 3` tools across requirements, request, host answer, hold, and release. Native `executeTool` completed the whole sequence, including stale `$400` refusal followed by an exact `$423` hold.
5. The visible buyer and host state changed after every call, no page errors were recorded, all 42 tests passed, and the production build succeeded.

The current compact contract has not yet been re-run through ChatGPT's model-driven in-app Browser, so the prior exact-client acceptance below must not be treated as proof of this revision. That rerun remains a hard E9 gate.

## ChatGPT in-app Browser native acceptance

On 2026-08-26 PT, the unified ChatGPT app 26.820.60940 with GPT-5.6 Sol connected its fresh built-in Browser task to `http://127.0.0.1:3000/` and completed the full typed core acceptance flow:

1. The visible page reported `Site Tools live` and `3 native Site Tools are available for this page state.`
2. The client discovered exactly `inspect_live_show`, `set_evidence_requirements`, and `inspect_current_lot`; the first and third were classified read-only and the middle tool as a write.
3. A native model-driven `set_evidence_requirements` call sent exactly `minLengthCm`, `maxLengthCm`, `requireVisibleEdgeEvidence`, and `forbidPriorBaseRepair`. The result explicitly listed maximum willingness to pay, private price ceiling, buyer profile, and urgency as not collected.
4. The valid tool surface changed with page state: 3 initial → 4 with `request_host_evidence` → 3 after queueing → 4 with `reserve_current_lot` after the host answer → 3 with `release_current_lot` while held → 4 after release.
5. Native `request_host_evidence({ kind: 'repair_history' })` joined seven labeled fixture signals, producing one normalized question for eight private decisions. One visible host answer supplied `Host live disclosure · source frame 00:31` and made the evidence 3/3 supported.
6. Native `reserve_current_lot({ expectedAllInPrice: 400 })` was refused because the exact current quote was `$423`; no hold was created. Retrying with `423` created local hold `hold-6`, took no payment, and exposed release instead of reserve.
7. Native `release_current_lot({})` succeeded and restored the prior reserve surface. The final page reported `Reservation released`, with no browser warnings or errors.
8. No confirmation UI surfaced for this reversible local, non-payment hold. This records the exact observed action; it does not override OpenAI's documented confirmations for purchases, communications, deletion, permissions, or other consequential operations.

The first read run exposed a judged-runtime compatibility bug: the callback supplied an options object without `signal`, while the provider used `options?.signal.throwIfAborted()`. The implementation now uses `options?.signal?.throwIfAborted()` and tests both omitted options and an empty options object. The rebuilt production server then passed the complete sequence above.

Seven machine-local acceptance screenshots are retained outside the repository, from initial discovery through released hold. The remaining exact-client questions are Voice-to-Site-Tools composition, repeated navigation/reconnect behavior, and a clean-room MBP run—not the core typed Site Tools flow.

## Public HTTPS, OIDC, and live vision acceptance

On 2026-08-26 PT, a reversible Vercel preview at `https://webmcp-evidence-preview.vercel.app` was connected to a Wrangler temporary Worker with the real Durable Object. This is acceptance infrastructure, not the final submission URL: Vercel Authentication protects the preview, and Cloudflare deletes an unclaimed temporary account after its claim window.

1. The live Worker health route and exact-origin room creation passed. Buyer and host tabs authenticated as distinct roles, recovered authoritative state across a Vercel redeployment, and synchronized the requirements, normalized request, fixture answer, hold, and release.
2. Vercel automatically supplied `VERCEL_OIDC_TOKEN`; a minimal AI SDK 7 text call reached Gateway. GPT-5.6 Sol and Luna both returned HTTP 403 with `RestrictedModelsError` before a provider call. This conflicts with broad free-credit wording, so live account behavior—not pricing-page inference—controls the deployment claim.
3. The current [Gateway model catalog](https://ai-gateway.vercel.sh/v1/models) and live [free-tier-filtered model browser](https://vercel.com/ai-gateway/models?freeTier=true) were re-read before probing. With zero-data-retention routing enforced, `alibaba/qwen3.7-flash` passed plain JPEG vision in 4.6 seconds and schema-validated JPEG vision in 4.0 seconds. Qwen 3.8 Flash's vision route was rate-limited, GLM 5.3 Flash timed out, and MiniMax M3 produced no structured output in the same bounded probes.
4. The deployed route recomputed the selected JPEG digest and returned a strict `alibaba/qwen3.7-flash` observation before its 20-second deadline. It correctly described Chromium's synthetic green camera test pattern, marked the snowboard base not visible, and made no repair-history claim. Host acceptance did not enable either publication action.
5. After the explicitly labeled deterministic fixture supplied positive evidence, native Chrome 151 discovered `inspect_live_show`, `set_evidence_requirements`, and `reserve_current_lot`; refused stale `$400`; held at exact `$423` without payment; replaced reserve with release; and released successfully. Inspection contained no JPEG data or private `$450` ceiling.
6. Chrome 151 required a serialized JSON string in `executeTool(tool, JSON.stringify(input))`. An object argument failed with `Failed to parse input arguments`. Tool registration reflected the state transition after the browser/React update tick.
7. Opening a fragment-bearing host invite without the preview bypass cookie redirected to Vercel login while leaving the fragment in the address bar. Although fragments do not cross the HTTP request boundary, redirect-page JavaScript can access them. Final preflight must prove that the host invite never crosses an authentication or cross-origin redirect.

Primary current sources: Vercel [AI Gateway authentication](https://vercel.com/docs/ai-gateway/authentication-and-byok), [pricing](https://vercel.com/docs/ai-gateway/pricing), [model browser](https://vercel.com/ai-gateway/models?freeTier=true), and [Vercel Authentication](https://vercel.com/docs/deployment-protection/methods-to-protect-deployments/vercel-authentication); Cloudflare [temporary deployments](https://developers.cloudflare.com/workers/configuration/previews/#temporary-deployments); Chrome [WebMCP guidance](https://developer.chrome.com/docs/ai/webmcp).

## Official documentation

| Resource                                                                                                         | Read | Decision-useful result                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------------------------------------------------------------------------------------------------------------- | ---: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [WebMCP specification repository](https://github.com/webmachinelearning/webmcp)                                  |  Yes | Canonical source, explainers, Web Platform Tests, and open issues. The API is changing quickly enough that same-day source checks matter.                                                                                                                                                                                                                                                              |
| [Current rendered WebMCP draft](https://webmachinelearning.github.io/webmcp/)                                    |  Yes | Draft dated August 26, 2026, re-read August 27. Native surface is `document.modelContext`; registration is async; execute receives an `AbortSignal`; tool names are 1–128 restricted ASCII characters; `title`, annotations, cross-document exposure, cancellation, and `toolchange` are specified. Declarative execution still contains open specification work.                                      |
| [Chrome WebMCP developer documentation](https://developer.chrome.com/docs/ai/webmcp)                             |  Yes | Chrome 149 origin trial and local flag, origin isolation, permissions policy, imperative and declarative APIs, and visible human-in-the-loop behavior.                                                                                                                                                                                                                                                 |
| [Chrome WebMCP origin trial](https://developer.chrome.com/blog/ai-webmcp-origin-trial)                           |  Yes | Establishes current Chrome enrollment/flag path; runtime behavior still must be tested.                                                                                                                                                                                                                                                                                                                |
| [Chrome secure tools guidance](https://developer.chrome.com/docs/ai/webmcp/secure-tools)                         |  Yes | Tool poisoning, output injection, intent misrepresentation, over-parameterization, and origin boundaries are first-class design concerns. Current recommended maxima are 30-character tool/parameter names, 500-character tool descriptions, 150-character parameter descriptions, and 1.5K-character results; all dynamic tools and common result states are enforced against those budgets in tests. |
| [Chrome WebMCP eval guidance](https://developer.chrome.com/docs/ai/webmcp/evals)                                 |  Yes | Evaluation sets should include the complete state-specific tool surface, direct and ambiguous prompts, ordered end-to-end cases, corrections, and mid-chain failures. The repository now carries a schema-locked Chrome-format corpus in `evals/`; provider-backed reports are still pending.                                                                                                          |
| [Chrome DevTools WebMCP panel](https://developer.chrome.com/docs/devtools/application/webmcp)                    |  Yes | The Application panel exposes available tools and schemas, invocation counters, and chronological invocation history. This is the preferred manual proof surface for final QA and a supporting demo shot, not a substitute for the agent-driven experience.                                                                                                                                            |
| [`webmcp-types`](https://github.com/webmachinelearning/webmcp-types)                                             |  Yes | The specification repository's current TypeScript package is 0.1.5. Its provider-facing declarations match current async registration, `title`, and execute cancellation and pass an isolated TypeScript 7 strict check with `skipLibCheck: false`. It does not yet declare the draft's in-page `executeTool` consumer method.                                                                         |
| [MCP `2026-07-28` versioning](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning)         |  Yes | Current MCP is a new stateless/per-request-metadata era; `2025-11-25` and earlier are initialization-based. A dual-era merchant can support both, but must select semantics from the request shape and fail version mismatches with the specified modern error.                                                                                                                                        |
| [MCP server discovery](https://modelcontextprotocol.io/specification/2026-07-28/server/discover)                 |  Yes | Modern servers must implement `server/discover`, returning supported versions, capabilities, identity, instructions, and optional cache metadata. The owned merchant implements this without making remote MCP a substitute for page WebMCP.                                                                                                                                                           |
| [MCP Streamable HTTP](https://modelcontextprotocol.io/specification/2026-07-28/basic/transports/streamable-http) |  Yes | Current HTTP is stateless POST-only and mirrors body metadata into required `MCP-Protocol-Version`, `Mcp-Method`, and conditional `Mcp-Name` headers; servers must validate header/body equality and the request `Origin`. The merchant tests every implemented header boundary.                                                                                                                       |
| [MCP tools](https://modelcontextprotocol.io/specification/2026-07-28/server/tools)                               |  Yes | Current list/call results use `resultType`, deterministic tool order, optional `ttlMs`/`cacheScope`, and human-in-the-loop guidance. Structured output should retain serialized text compatibility.                                                                                                                                                                                                    |

## OpenAI resources

| Resource                                                                                                                                                      | Read | Decision-useful result                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [WebMCP app showcase](https://developers.openai.com/showcase?view=webmcp-apps)                                                                                |  Yes | Generic shopping, CRUD, editing, exploration, and game examples make a normal tool-enabled site insufficiently novel.                                                                                                                                                                                    |
| [ChatGPT Sites](https://learn.chatgpt.com/docs/sites?surface=app)                                                                                             |  Yes | A possible host, but not required. Current Sites policies make it a poor primary host for a commerce entry that enables payments.                                                                                                                                                                        |
| [OpenAI Site Tools / WebMCP](https://learn.chatgpt.com/docs/webmcp)                                                                                           |  Yes | ChatGPT and the page share the live page and signed-in session. Tools are page-bound and untrusted; calls receive safety review; normal confirmations apply to purchases, messages, deletion, and permissions. Use the latest desktop app with GPT-5.6 Sol or Terra; Luna currently has WebMCP disabled. |
| [OpenAI MCP API documentation](https://developers.openai.com/api/docs/mcp)                                                                                    |  Yes | This describes local/remote MCP servers, not the page-bound Site Tools test path. It is relevant to interoperability, but it cannot prove WebMCP behavior in ChatGPT's browser.                                                                                                                          |
| [ChatGPT Voice](https://learn.chatgpt.com/docs/features/voice)                                                                                                |  Yes | Voice works in Chat, Work, and Codex in the desktop app and through paired iOS Remote, but the documentation does not explicitly establish that a Voice session can invoke Site Tools in the built-in browser. Treat the composition as an exact-runtime experiment and retain typed input.              |
| [Realtime API](https://developers.openai.com/api/docs/guides/realtime) and [GPT-Realtime-2.1](https://developers.openai.com/api/docs/models/gpt-realtime-2.1) |  Yes | Browser voice agents can use WebRTC for low-latency audio, reasoning, speech, and tool calls. The current model accepts text, audio, and image input but not video; camera understanding therefore requires explicit sampled frames or a separate processing path.                                       |

## Cloudflare resources

| Resource                                                                                                                                                                     | Read | Decision-useful result                                                                                                                                                                                                                                                 |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Cloudflare WebMCP overview](https://blog.cloudflare.com/webmcp/)                                                                                                            |  Yes | Agent Readiness can inject a same-origin bridge and optional Content Credentials or Site MCP Server packs. The C2PA pack currently parses credentials but reports `signatureVerified: false`; do not represent parsing as cryptographic verification.                  |
| [WebMCP on Browser Run](https://developers.cloudflare.com/browser-run/features/webmcp/)                                                                                      |  Yes | Cloudflare's lab currently runs Chrome Beta and exposes `navigator.modelContextTesting`; its documented automation can compose with Chrome DevTools MCP. It is useful as an independent lab, but not a replacement for final native-Chrome and ChatGPT proof.          |
| [Coffee-store demo](https://webmcp-coffee.jilles.fyi/)                                                                                                                       |  Yes | Helpful implementation reference; ordinary product search/cart is already well represented.                                                                                                                                                                            |
| [Challenge example](https://webmcp-challenge.examples.workers.dev/)                                                                                                          |  Yes | Supporter overview and demonstration, not an architecture mandate.                                                                                                                                                                                                     |
| [Workers React starter](https://github.com/cloudflare/agents/tree/main/examples/webmcp-react)                                                                                |  Yes | Strong patterns: direct tool registration, runtime validation, shared human/tool actions, abort cleanup, unsupported-state UI, and Vitest.                                                                                                                             |
| [Cloudflare Pages and Workers](https://developers.cloudflare.com/pages/)                                                                                                     |  Yes | Viable primary hosting path. Current Vite integration, Durable Objects, Browser Run, and RealtimeKit are a coherent stack for the live-market hypothesis.                                                                                                              |
| [Durable Object class exports](https://developers.cloudflare.com/durable-objects/reference/durable-objects-migrations/)                                                      |  Yes | New Worker namespaces should use declarative `exports` and SQLite rather than inventing a legacy migration chain. The room and merchant Workers use this current lifecycle model; it is mutually exclusive with legacy `migrations`.                                   |
| [SQLite-backed Durable Object storage](https://developers.cloudflare.com/durable-objects/api/sqlite-storage-api/)                                                            |  Yes | Current docs (updated August 25) recommend SQLite-backed classes and confirm strongly consistent private per-object storage, KV-compatible APIs, alarms, SQL, and point-in-time recovery. The merchant uses bounded KV-style records and alarms on the SQLite backend. |
| [Cloudflare RealtimeKit](https://developers.cloudflare.com/realtime/realtimekit/) and [SDK selection](https://developers.cloudflare.com/realtime/realtimekit/sdk-selection/) |  Yes | Current beta SDKs cover web, Android, iOS, and React Native voice/video rooms. This can make host/buyer media multi-device, but it does not extend the prescribed ChatGPT/Chrome WebMCP client support to those devices.                                               |

## Vercel resources

| Resource                                                            | Read | Decision-useful result                                                                                                                                             |
| ------------------------------------------------------------------- | ---: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [Vercel commerce source](https://github.com/vercel/shop)            |  Yes | Current, polished Next.js storefront reference. Its WebMCP commerce path delegates to Shopify's Hydrogen scripts rather than defining a novel collaboration layer. |
| [WebMCP implementation PR](https://github.com/vercel/shop/pull/498) |  Yes | Confirms the move from hand-written storefront tools to Shopify-provided tools. Useful precedent, not sufficient WebMCP depth for our entry.                       |
| [Live storefront](https://template.vercel.shop/)                    |  Yes | Establishes the quality floor for a commerce UI.                                                                                                                   |
| [Vercel pricing](https://vercel.com/pricing)                        |  Yes | Hosting option only; no reason to pair it with another primary host.                                                                                               |

## Shopify resources

| Resource                                                                                          | Read | Decision-useful result                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------- | ---: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [Shopify WebMCP tools](https://shopify.dev/docs/api/web-mcp)                                      |  Yes | Every Liquid storefront and Hydrogen preview storefront can expose authoritative catalog, product, cart, checkout, order, and policy tools. Shopify should own commodity commerce while our direct native tools own the novel live collaboration.                                                                        |
| [Shopify CLI Store commands](https://shopify.dev/docs/api/shopify-cli/store)                      |  Yes | Current CLI `4.7.0` can create free temporary preview stores, persist direct Admin sessions, query/mutate development stores, and open previews. The preview path is excellent for agent-driven integration testing but is not a stable judge origin.                                                                    |
| [`store create preview`](https://shopify.dev/docs/api/shopify-cli/store/store-create-preview)     |  Yes | Directly created an isolated no-card preview store and an original product. Shopify's current onboarding guidance says preview stores cannot accept real orders/payments and preview links expire, so this is a control surface rather than the final merchant.                                                          |
| [Development stores](https://shopify.dev/docs/storefronts/themes/tools/development-stores)        |  Yes | Free dev stores support unlimited test products/orders and custom apps but are always password protected until transfer/paid activation. Submission credentials are allowed, but the extra judge step and missing local password keep this off the critical path for now.                                                |
| [Storefront MCP](https://shopify.dev/docs/apps/build/storefront-mcp/servers/storefront)           |  Yes | Shopify exposes unauthenticated store-specific MCP and UCP Catalog endpoints plus Cart handoff, subject to store restrictions. A password-gated dev store still redirects before the endpoint, so “unauthenticated protocol” does not imply “unrestricted merchant.”                                                     |
| [Shopify agentic commerce](https://shopify.dev/docs/agents)                                       |  Yes | Catalog and commerce-agent capabilities are broader than WebMCP alone; use only where they create a real merchant/agent boundary.                                                                                                                                                                                        |
| [Agent profiles and negotiation](https://shopify.dev/docs/agents/profiles)                        |  Yes | Current fixtures and live merchants use UCP `2026-04-08`, with the public profile URI carried in `meta.ucp-agent.profile`. Negotiate the advertised shape instead of assuming the newest draft.                                                                                                                          |
| [Cart MCP](https://shopify.dev/docs/agents/carts-and-checkout/cart-mcp)                           |  Yes | Anonymous JSON-RPC create/get/update/cancel runs at the merchant's `/api/ucp/mcp`; responses place merchant-authoritative Cart state in `structuredContent`, while cancellation requires a unique idempotency key.                                                                                                       |
| [Authentication and rate limits](https://shopify.dev/docs/agents/profiles/auth-and-rate-limiting) |  Yes | Anonymous access is enough for Catalog, Cart, and checkout handoff, but not direct checkout completion or Orders. The hero should stop at a human continuation rather than imply trusted payment authority.                                                                                                              |
| [Shopify UCP quickstart](https://shopify.dev/docs/agents/get-started/quickstart)                  |  Yes | Officially recommends the UCP CLI and Shopify AI Toolkit. Local verification pins Shopify CLI `4.7.0`, `@shopify/ucp-cli` `0.7.0`, the official Codex plugin revision `11c74d6b`, and UCP skill metadata `1.9.1`; every UCP probe opted out of instrumentation.                                                          |
| [UCP `2026-04-08` Cart MCP binding](https://ucp.dev/2026-04-08/specification/cart-mcp/)           |  Yes | Confirms MCP transport, profile negotiation, and Cart semantics at the exact production version selected by Shopify.                                                                                                                                                                                                     |
| [`@ucp-js/sdk` `0.4.6`](https://www.npmjs.com/package/@ucp-js/sdk)                                |  Yes | Rejected after direct evaluation: raw Node 26 ESM import fails on extensionless internal imports, and its discovery validator rejects Shopify's current official/live profile shape. Use narrow Zod 4 wire validators modeled on the current spec and official CLI instead; re-evaluate when a compatible release ships. |

## Chrome resources

| Resource                                                                                |                                           Read | Decision-useful result                                                                                                                                                                                    |
| --------------------------------------------------------------------------------------- | ---------------------------------------------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`use-webmcp-tool`](https://www.npmjs.com/package/use-webmcp-tool)                      | Yes, including installed source/types at 0.2.0 | React-friendly, but currently omits `title` and the execute cancellation options and does not await async registration. Do not use it for the canonical challenge path without an update or local repair. |
| [WebMCP explainer](https://github.com/webmachinelearning/webmcp/blob/main/README.md)    |                                            Yes | Human-agent shared-page rationale and API intent. Current rendered spec remains authoritative for exact signatures.                                                                                       |
| [Angular WebMCP](https://angular.dev/ai/webmcp)                                         |                                            Yes | Useful framework-native precedent; not a reason to switch frameworks.                                                                                                                                     |
| [Chrome WebMCP evals](https://developer.chrome.com/docs/ai/webmcp/evals)                |                                            Yes | Test direct and ambiguous prompts, state-dependent tool sets, full tasks, correction, and recovery—not only handler functions.                                                                            |
| [Chrome DevTools WebMCP](https://developer.chrome.com/docs/devtools/application/webmcp) |                                            Yes | Application-panel inspection exposes registered tools, invocation state, input, output, and manual calls. Primary Chrome debugging surface.                                                               |
| [Modern Web Guidance](https://github.com/GoogleChrome/modern-web-guidance)              |                                            Yes | Useful implementation patterns, but its current package still contains stale Chrome/version and fallback claims. Do not treat it as version authority.                                                    |
| [GoogleChromeLabs demos](https://github.com/GoogleChromeLabs/webmcp-tools)              |                                            Yes | Shows the crowded baseline of conventional tasks. Mine patterns, not concepts.                                                                                                                            |

## Render and Netlify resources

| Resource                                                                                                                                                 | Read | Decision-useful result                                                                                                                                                                         |
| -------------------------------------------------------------------------------------------------------------------------------------------------------- | ---: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Render Workflows](https://render.com/workflows) and [docs](https://render.com/docs/workflows)                                                           |  Yes | Strong for durable asynchronous video/AI jobs; not a natural authoritative realtime-room server.                                                                                               |
| [Render templates](https://render.com/templates) and [credits documentation](https://render.com/docs/credits)                                            |  Yes | Optional implementation/hosting resources; no external credit claim made.                                                                                                                      |
| [Netlify](https://www.netlify.com/), [getting started](https://docs.netlify.com/get-started/), and [WebMCP starter](https://webmcp-starter.netlify.app/) |  Yes | Strong Agent Experience lens—discover, invoke, recover—and a useful starter. No unique capability found that beats Cloudflare for the live realtime hypothesis. No external credit claim made. |

## Important source conflicts

1. **Video requirement:** resources FAQ line 194 says "there's no video"; line 208 and the Official Rules require a public video under three minutes. Treat video as required.
2. **API surface:** some August 2026 partner materials still show `navigator.modelContext` or `navigator.modelContextTesting`; the current specification, Chrome documentation, and OpenAI example use `document.modelContext`.
3. **Browser versions:** older supporter examples cite Chrome 146. The challenge requires Chrome 149+, and the installed Chrome is 151. Its tested consumer surface currently differs from the August 26 draft; exact runtime wins over prose.
4. **MCP versus WebMCP:** OpenAI's remote MCP docs describe a server connection that can work without an open page. ChatGPT Site Tools discover page-owned WebMCP only while the page is present. Test and describe them separately.
5. **Helper packages versus draft:** the current latest Google hook (0.2.0) and MCP-B types (5.0.1) lag the August 26 execute signature. The specification project's `webmcp-types` 0.1.5 matches the provider callback but lacks the current in-page `executeTool` consumer method. Package affiliation or recency cannot override the draft and judged runtime.

## Chrome inspection path

The user-linked [WebMCP Extension](https://chromewebstore.google.com/detail/webmcp-extension/jigokfbbpcdckjmhbgapmikncfihboec) is an independent extension by Amrin Grewal (version 1.0, updated April 22, 2026). It can list, execute, and log tools, but its documentation refers to Chrome 146 and the older `navigator.modelContextTesting` surface. Use it as a secondary view only.

The primary path is Chrome 151 with the WebMCP testing flag, Chrome DevTools' native WebMCP pane, and Google's [Model Context Tool Inspector](https://chromewebstore.google.com/detail/webmcp-model-context-tool/gbpdfapgefenggkahomfgkhfehlcenpd) if an extension is useful. The Google inspector is version 1.9.13, updated August 19, 2026, offered by Google, and requires Chrome 150+. Its own listing warns that it is a development aid rather than a production security boundary. Neither extension was installed during this pass.

## Chrome 151 isolated native smoke test

On 2026-08-26 PT, a temporary page registered an imperative `add_numbers` tool and was served from localhost. Chrome 151.0.7922.174 ran headlessly in fresh isolated profiles so the test did not alter the normal Chrome profile.

- Negative control without the feature flag: `document.modelContext` was absent.
- With `--enable-features=WebMCP`: `document.modelContext` was present, `registerTool()` returned a promise, awaited registration succeeded, and `getTools()` returned `add_numbers`.
- Current-draft-style `executeTool(registeredTool, object)` failed with `Failed to parse input arguments`.
- Chrome-151-style `executeTool(registeredTool, JSON.stringify(object))` succeeded and returned the serialized tool result with sum `42`.
- The registered execute callback received only its input object; its second cancellation-options argument was `undefined`, despite the August 26 draft specifying `(input, { signal })`.
- Observed function arities were `registerTool.length === 1`, `getTools.length === 0`, and `executeTool.length === 2`.

This is evidence of a real draft/runtime skew, not a reason to target stale APIs exclusively. Challenge code should keep provider registration current, tolerate a missing callback-options argument where the judged runtime requires it, and isolate any in-page consumer adapter. The exact ChatGPT Site Tools behavior remains higher-priority evidence. Headless execution is supplementary because Chrome's own guidance describes WebMCP as primarily human-in-the-loop.

The reproducible fixture is [research/webmcp-runtime-smoke](research/webmcp-runtime-smoke/README.md). It is research evidence, not entry code. The four temporary Chrome profiles and superseded ignored fixture copy were moved to Trash after testing.

## Exact runtime acceptance matrix

Run this matrix at the walking skeleton, every material tool-surface change, release candidate, and final deployed build.

| Behavior                                                                                | Chrome native                                                |                               ChatGPT Site Tools |
| --------------------------------------------------------------------------------------- | ------------------------------------------------------------ | -----------------------------------------------: |
| Feature detection and async registration succeed                                        | Pass: Chrome 150 flow + Chrome 151 positive/negative gates   |                           Pass: app 26.820.60940 |
| Read tool is discovered and returns current visible state                               | Pass: isolated Chrome 150                                    |                           Pass: two native reads |
| Write tool changes the same visible UI                                                  | Pass: isolated Chrome 150                                    |       Pass: requirements, request, hold, release |
| Dynamic registration/removal is observed after state changes                            | Pass: isolated Chrome 150                                    |             Pass: observed 3 → 4 → 3 → 4 → 3 → 4 |
| Ambiguous prompt selects or clarifies correctly                                         | Pending: no model-driven consumer                            |                                          Pending |
| Invalid/stale input is rejected with actionable recovery                                | Pass: isolated Chrome 150 stale quote                        |        Pass: stale `$400` refused against `$423` |
| Abort/cancellation stops work and leaves coherent state                                 | Unit pass; native cancellation still pending                 |                                          Pending |
| Consequential action produces the real confirmation behavior                            | N/A for the reversible, non-payment hold fixture             | Observed: no prompt for local hold; purchase N/A |
| Navigation, refresh, BFCache, and reconnect do not expose stale tools                   | Pending                                                      |                                          Pending |
| Tool output and page content cannot silently escalate authority                         | Contract pass; adversarial native prompt test still pending  |                                          Pending |
| Camera is explicit; continuous feed stays local; selected frame publication is explicit | Pass: synthetic video + strict schema/native inspect         |                                          Pending |
| Selected image is visible on-page but omitted from Site Tool payloads                   | Pass: buyer render + native audit result without image bytes |                                          Pending |
| Camera provenance and host review survive dynamic hold/release lifecycle                | Pass: native inspect → exact hold → release                  |                                          Pending |
| AI proposal is non-authoritative and has an honest no-auth fallback                     | Pass: API/component/browser contract; live auth still open   |                                          Pending |
| Ordinary-browser fallback remains fully usable                                          | Pass: desktop and 390 px                                     |                                              N/A |

Record browser/app versions, model, URL/revision, prompt, expected result, observed result, screenshots/trace, and any failure. A passing development shim is supplementary evidence only.
