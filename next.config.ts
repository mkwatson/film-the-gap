import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  allowedDevOrigins: ['opens-mac-mini.taild4d569.ts.net'],
  reactStrictMode: true,
  typedRoutes: true,
};

export default nextConfig;
